# CIFS/SMB mounts for Sizemore-Server
# These entries belong in /etc/fstab

# MediaDrive — Windows SMB share from 10.0.0.142 (mcshmingus)
# Credentials file: /etc/samba/credentials-mediadrive (chmod 600, owned by root)
//10.0.0.142/MediaDrive  /media/downloads  cifs  credentials=/etc/samba/credentials-mediadrive,vers=3.0,_netdev,auto  0  0
